#!/bin/sh

# This script is run automatically when running npm install.

isApp=$(./config/isApp.rb)
if [ $isApp == true ]; then
    (sh ./config/configureSystem.command && sh ./config/configureProject.command) ||
        echo 'Warning: unable to configure project with UFSLibrary'
fi
