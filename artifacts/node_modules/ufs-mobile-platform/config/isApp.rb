#!/usr/bin/env ruby

# This script returns true if running from an app (not the platform project).

script_dir = File.expand_path(File.dirname(__FILE__))
if script_dir.include?('node_modules')
    puts true
else
    puts false
end
