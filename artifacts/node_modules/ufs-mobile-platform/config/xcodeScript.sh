# This script prepares the library and launches React Package Manager
# if not already running.

CONFIG_ROOT="$PROJECT_DIR/../node_modules/ufs-mobile-platform/config"
sh "$CONFIG_ROOT/prepareLibrary.sh"
sh "$CONFIG_ROOT/launchPackager.sh"
