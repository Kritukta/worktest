import { NativeModules } from 'react-native';
/**
 * В библиотеке встроена палитра цветов. Для использования необходимо импортировать объект colors из ufs-mobile-platform:
 * ```
 * import {
 *    colors
 * } from 'ufs-mobile-platform';
 * ```
 *
 * Свойства объекта представляют из себя строки, которые можно использовать в React styles.
 * @category Nonvisual
 */
export default class UFSColor {
}
export const Color = NativeModules.UFSColors;
//# sourceMappingURL=UFSColor.js.map