import { NativeModules } from 'react-native';
const UserInfoService = NativeModules.UFSUserInfoServiceBridge;
/**
 * Компонент предназначен для получения данных учётной записи пользователя.
 * @category Nonvisual
 */
export default class UFSUserInfoService {
    /**
     * Метод возвращает последний сохраненный логин пользователя.
     *
     * #### Обратите внимание
     * Логин обновляется после успешного прохождения процесса аутентификации.
     * Логин не удаляется при потере сессии, выходе из приложения или начале работы ММВ.
     *
     * **Входные параметры:**
     * - `callback` - функция, в качестве параметра которой будет передан логин пользователя в виде строки.
     * В случае отсутствия авторизованного пользователя, параметр `login` будет содержать значение `null`.
     *
     * **Возвращаемые значения:**
     * отсутствуют
     * @static
     */
    static login(callback) {
        UserInfoService.login(callback);
    }
}
//# sourceMappingURL=UFSUserInfoService.js.map