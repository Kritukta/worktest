/// <reference types="react" />
import React, { Component } from 'react';
export interface Props extends React.Props<UFSColumnManager> {
    /**
     * Размер (ширина) колонки. Допустимые значения от 1 до 12.
    */
    xs: number;
    /**
     * Сдвиг колонки вправо. Увеличивает left margin колонки на * колонок. Например, `xsOffset`=4 отодвинет колонку
     * со свойством `xs`=4 на 4 колонки.
    */
    xsOffset?: number;
    /**
     * Флаг, отвечающий за скрытие колонки.
    */
    xsHidden?: boolean;
    /**
     * Идентификатор для UI-тестирования.
    */
    testID?: string;
}
/**
 * Компонент используется для позиционирования элементов при помощи сетки из 12 колонок, которая тянется на 100%.
 * Для того чтобы вставить колонку нужного размера, требуется 12 поделить на количество желаемых колонок, т.е. если нужно
 * вставить две колонки, то 12 надо поделить на 2 и полученный результат прописать в колонку.
 * Используется только в качестве дочернего для `Row`.
 * @category Visual/Grid
 */
export default class UFSColumnManager extends Component<Props, {}> {
    render(): JSX.Element;
}
