import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Компонент используется для позиционирования элементов при помощи сетки из 12 колонок, которая тянется на 100%.
 * Корневой компонент сетки, качестве дочернего элемента используется только компонеты `Row`.
 * @category Visual/Grid
 */
export default class UFSGridManager extends Component {
    render() {
        return (React.createElement(UFSGrid, {testID: this.props.testID}, this.props.children));
    }
}
const UFSGrid = requireNativeComponent('UFSGridView');
//# sourceMappingURL=UFSGrid.js.map