/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * В качестве дочерних компонентов принимается `Col`.
     */
    children?: ReactNode;
}
/**
 * Компонент используется только в качестве дочернего для `Grid`.
 * @category Visual/Grid
 */
export default class UFSRowManager extends Component<Props, {}> {
    render(): JSX.Element;
}
