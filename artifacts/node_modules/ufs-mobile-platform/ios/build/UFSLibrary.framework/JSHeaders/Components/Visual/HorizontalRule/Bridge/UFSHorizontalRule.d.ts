/// <reference types="react" />
/// <reference types="react-native" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
export interface Props {
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
    /**
     * Стили React.
     */
    style?: ViewStyle;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
}
/**
 * Используется для разделения полотна контента на смысловые блоки.
 *
 * <br/>
 *
 * В отличии от группировки по секциям контент, разделяемый пунктирной чертой, тесно связан между собой по смыслу и образует цепочку.
 * @category Visual
 */
export default class UFSHorizontalRuleManager extends Component<Props, {}> {
    render(): JSX.Element;
}
