/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Icon`
     */
    children?: ReactNode;
}
/**
 * Контейнер содержит в себе дополнительные компоненты для отображения в левой части компонента ввода текста.
 * @category Visual
 */
export default class UFSInputLeftContainerManager extends Component<Props, {}> {
    render(): JSX.Element;
}
export declare const renderInputLeftContainer: (children: ReactNode) => React.ReactChild;
