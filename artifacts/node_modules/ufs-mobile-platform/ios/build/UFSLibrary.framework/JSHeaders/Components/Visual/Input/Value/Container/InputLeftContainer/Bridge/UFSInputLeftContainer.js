import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Контейнер содержит в себе дополнительные компоненты для отображения в левой части компонента ввода текста.
 * @category Visual
 */
export default class UFSInputLeftContainerManager extends Component {
    render() {
        return (React.createElement(UFSInputLeftContainer, null, this.props.children));
    }
}
const UFSInputLeftContainer = requireNativeComponent('UFSInputLeftContainerView');
export const renderInputLeftContainer = (children) => {
    if (!children) {
        return null;
    }
    const childrenArray = React.Children.toArray(children);
    const leftContainers = childrenArray.filter(child => {
        if (child.type === UFSInputLeftContainerManager) {
            return true;
        }
        else {
            return false;
        }
    });
    var leftContainer = null;
    if (leftContainers.length == 1) {
        leftContainer = leftContainers[0];
    }
    return leftContainer;
};
//# sourceMappingURL=UFSInputLeftContainer.js.map