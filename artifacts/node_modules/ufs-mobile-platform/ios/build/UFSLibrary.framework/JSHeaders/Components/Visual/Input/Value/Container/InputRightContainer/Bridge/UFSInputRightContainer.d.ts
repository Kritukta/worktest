/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Button`
     */
    children?: ReactNode;
}
/**
 * Контейнер содержит в себе дополнительные компоненты для отображения в правой части компонента ввода текста.
 * @category Visual
 */
export default class UFSInputRightContainerManager extends Component<Props, {}> {
    render(): JSX.Element;
}
export declare const renderInputRightContainer: (children: ReactNode) => React.ReactChild;
