import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Контейнер содержит в себе дополнительные компоненты для отображения в правой части компонента ввода текста.
 * @category Visual
 */
export default class UFSInputRightContainerManager extends Component {
    render() {
        return (React.createElement(UFSInputRightContainer, null, this.props.children));
    }
}
const UFSInputRightContainer = requireNativeComponent('UFSInputRightContainerView');
export const renderInputRightContainer = (children) => {
    if (!children) {
        return null;
    }
    const childrenArray = React.Children.toArray(children);
    const rightContainers = childrenArray.filter(child => {
        if (child.type === UFSInputRightContainerManager) {
            return true;
        }
        else {
            return false;
        }
    });
    var rightContainer = null;
    if (rightContainers.length == 1) {
        rightContainer = rightContainers[0];
    }
    return rightContainer;
};
//# sourceMappingURL=UFSInputRightContainer.js.map