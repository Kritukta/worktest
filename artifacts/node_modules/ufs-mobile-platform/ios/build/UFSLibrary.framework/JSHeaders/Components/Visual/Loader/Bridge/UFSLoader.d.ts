/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * @private
     */
    children?: ReactNode;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
}
/**
 * Используется для уведомления пользователя о необходимости ожидания завершения активности.
 * @category Visual
 */
export default class UFSLoaderManager extends Component<Props, {}> {
    render(): JSX.Element;
}
