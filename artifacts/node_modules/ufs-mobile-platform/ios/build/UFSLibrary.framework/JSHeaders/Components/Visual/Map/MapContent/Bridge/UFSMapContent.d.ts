/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Marker`;
     */
    children?: ReactNode;
}
/**
 * Используется как контейнер для компонентов `Marker`.
 * @category Visual/Map
 */
export default class UFSMapContentManager extends Component<Props, {}> {
    render(): JSX.Element;
}
