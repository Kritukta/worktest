/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `MapTrafficButton`;
     * - `MapZoomInButton`;
     * - `MapZoomOutButton`;
     * - `MapLocateButton`;
     */
    children?: ReactNode;
}
/**
 * Используется как контейнер для компонентов Marker и не имеет никаких свойств.
 * @category Visual/Map
 */
export default class UFSMapControlsManager extends Component<Props, {}> {
    render(): JSX.Element;
}
