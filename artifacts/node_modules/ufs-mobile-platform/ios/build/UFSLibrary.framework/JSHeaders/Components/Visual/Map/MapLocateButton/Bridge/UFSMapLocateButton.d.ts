/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { Coordinate } from '../../../../JSCore/Common/UFSInterfaces';
export interface MapLocaleButtonEvent {
    nativeEvent: {
        coordinate: Coordinate;
        recommendedZoom: number;
    };
}
export interface Props {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
export interface PublicProps extends Props {
    /**
     * Вызывается при нажатии на кнопку.
     * В коллбек передается координата текущего местоположение пользователя, а также рекомендуемый уровень зума.
     */
    onClick?: (coordinate: Coordinate, zoom: number) => void;
}
export interface PrivateProps extends Props {
    onClick?: (event: MapLocaleButtonEvent) => void;
}
/**
 * Используется для получения текущей координаты пользователя.
 * @category Visual/Map
 */
export default class UFSMapLocateButtonManager extends Component<PublicProps, {}> {
    onClick: (event: MapLocaleButtonEvent) => void;
    render(): JSX.Element;
}
