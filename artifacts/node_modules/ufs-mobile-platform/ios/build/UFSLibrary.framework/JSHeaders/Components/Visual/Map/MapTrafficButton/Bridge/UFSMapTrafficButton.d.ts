/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Вызывается при нажатии на кнопку.
     */
    onClick?: () => void;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Используется для отображения текущего уровня пробок.
 * Уровень пробок автоматически обновляется когда свойство `traffic` у компонента `Map` имеет значение `true`.
 * @category Visual/Map
 */
export default class UFSMapTrafficButtonManager extends Component<Props, {}> {
    render(): JSX.Element;
}
