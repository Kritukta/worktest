import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Используется для отображения текущего уровня пробок.
 * Уровень пробок автоматически обновляется когда свойство `traffic` у компонента `Map` имеет значение `true`.
 * @category Visual/Map
 */
export default class UFSMapTrafficButtonManager extends Component {
    render() {
        return (React.createElement(UFSMapTrafficButton, {onClick: this.props.onClick, testID: this.props.testID}));
    }
}
const UFSMapTrafficButton = requireNativeComponent('UFSMapTrafficButtonView');
//# sourceMappingURL=UFSMapTrafficButton.js.map