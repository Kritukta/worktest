/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Вызывается при нажатии на кнопку.
     */
    onClick?: () => void;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Используется для увеличения уровня зума.
 * @category Visual/Map
 */
export default class UFSMapZoomInButtonManager extends Component<Props, {}> {
    render(): JSX.Element;
}
