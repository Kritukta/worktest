/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Вызывается при нажатии на кнопку.
     */
    onClick?: () => void;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Используется для уменьшения уровня зума.
 * @category Visual/Map
 */
export default class UFSMapZoomOutButtonManager extends Component<Props, {}> {
    render(): JSX.Element;
}
