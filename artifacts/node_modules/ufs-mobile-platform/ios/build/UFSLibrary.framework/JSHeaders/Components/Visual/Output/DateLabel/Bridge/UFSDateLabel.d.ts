/// <reference types="react" />
import { Component } from 'react';
import { CommonOutputProps } from '../../../../JSCore';
export declare const UFSDateFormat: any;
export interface Props extends CommonOutputProps {
    /**
     * Дата.
     */
    date: Date;
    /**
     * Указанный текст будет выведен в виде подписи над временем или слева от него.
     */
    label?: string;
    /**
     * Делает компонент блочным (по умолчанию `false`).
     */
    block?: boolean;
    /**
     * Однострочное отображение (по умолчанию `false`).
     */
    oneline?: boolean;
    /**
     * Формат вывода даты. Определяется свойствами интерфейса DateLabelFormat. Доступные значения:
     * - `MONTH`;
     * - `STANDARD` (значение по умолчанию);
     * - `STANDARD_WEEK_DAY`;
     *
     * Значение по умолчанию `STANDARD`.
     */
    format?: number;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
}
export interface PrivateProps extends Props {
    hasLabelHintIcon: boolean;
    hasTitleHintIcon: boolean;
}
/**
 * Компонент используется для вывода даты в заданном формате.
 * @category Visual/Output
 */
export default class UFSDateLabelManager extends Component<Props, {}> {
    static defaultProps: {
        label: string;
        block: boolean;
        oneline: boolean;
        format: any;
    };
    render(): JSX.Element;
}
