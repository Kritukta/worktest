/// <reference types="react" />
import { Component } from 'react';
import { CommonOutputProps } from '../../../../JSCore';
export interface Props extends CommonOutputProps {
    /**
     * Величина, к которой применяется маска.
     */
    value: string;
    /**
     * Маска для поля.
     */
    mask: string;
    /**
     * Делает компонент блочным (по умолчанию `false`).
     */
    block?: boolean;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
}
export interface PrivateProps extends Props {
    label: string;
    hasLabelHintIcon: boolean;
    hasTitleHintIcon: boolean;
}
/**
 * Компонент предназначен для вывода текста по маске.
 *
 * <br/>
 *
 * ##### Особенности
 * Для маскирования `value` используются специальные. Доступные значения:
 * - `1` - разрешает ввод только цифр
 * - `a` - только букв
 * - `*` - любых символов
 * @category Visual/Output
 */
export default class UFSMaskedTextManager extends Component<Props, {}> {
    static defaultProps: {
        block: boolean;
    };
    render(): JSX.Element;
}
