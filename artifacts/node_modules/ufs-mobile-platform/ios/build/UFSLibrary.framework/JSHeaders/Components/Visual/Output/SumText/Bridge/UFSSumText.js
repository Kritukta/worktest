import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
import { renderHintIcons } from '../../../HintIcon/HintIcon/Bridge/UFSHintIcon';
import { hasLabelHintIcon, hasTitleHintIcon } from '../../Common';
/**
 * Компонент предназначен для вывода денежных сумм с указанием валюты.
 *
 * <br/>
 *
 * ##### Особенности
 * Денежная сумма и наименование валюты отображаются одинаковым кеглем.
 * Сумма всегда содержит два знака после запятой: при необходимости она либо округляется, либо дополняется нулями.
 * Тысячные разряды в целой части автоматически разделяются пробелами.
 * @category Visual/Output
 */
export default class UFSSumTextManager extends Component {
    render() {
        const hintIcons = renderHintIcons(this.props.children, 'SumText', 2);
        return (React.createElement(UFSSumText, {label: this.props.label, fractionalDigitsCount: this.props.fractionalDigitsCount, currency: this.props.currency, value: this.props.value, block: this.props.block, small: this.props.small, testID: this.props.testID, hasLabelHintIcon: hasLabelHintIcon(hintIcons), hasTitleHintIcon: hasTitleHintIcon(hintIcons)}, hintIcons));
    }
}
UFSSumTextManager.defaultProps = {
    label: "",
    currency: "",
    block: false,
    small: false
};
const UFSSumText = requireNativeComponent('UFSSumTextView');
//# sourceMappingURL=UFSSumText.js.map