/// <reference types="react" />
import { Component } from 'react';
import { CommonOutputProps } from '../../../../JSCore';
export interface Props extends CommonOutputProps {
    /**
     * Компонент используется для вывода времени.
     */
    value: Date;
    /**
     * Указанный текст будет выведен в виде подписи над временем или слева от него в соответствии со значением свойства `oneline`.
     */
    label?: string;
    /**
     * Делает компонент блочным (по умолчанию `false`).
     */
    block?: boolean;
    /**
     * Однострочное отображение (по умолчанию `false`).
     */
    oneline?: boolean;
    /**
     * Показывать секунды (по умолчанию `false`).
     */
    showSeconds?: boolean;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
}
export interface PrivateProps extends Props {
    hasLabelHintIcon: boolean;
    hasTitleHintIcon: boolean;
}
/**
 * Компонент используется для вывода времени.
 * @category Visual/Output
 */
export default class UFSTimeManager extends Component<Props, {}> {
    static defaultProps: {
        label: string;
        block: boolean;
        oneline: boolean;
        showSeconds: boolean;
    };
    render(): JSX.Element;
}
