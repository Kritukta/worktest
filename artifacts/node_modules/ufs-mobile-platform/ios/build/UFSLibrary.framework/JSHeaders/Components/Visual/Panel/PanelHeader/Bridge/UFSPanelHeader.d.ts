/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
export interface Props extends React.Props<UFSPanelHeaderManager> {
    /**
     * Заголовок, информирующий о характере возникшей проблемы.
     */
    header?: string;
    /**
     * Атрибут, устанавливающий признак наличия иконки в хедере.
     */
    hasIcon?: boolean;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние JSX-компоненты
     * @private
     */
    children?: ReactNode;
}
/**
 * Компонент является внутренним и используется только в качестве дочернего для `Panel`.
 * Использование его отдельно в прикладных проектах не предусмотрено.
 * @private
 * @category Visual/Panel
 */
export default class UFSPanelHeaderManager extends Component<Props, {}> {
    render(): JSX.Element;
}
