import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Компонент является внутренним и используется только в качестве дочернего для `Panel`.
 * Использование его отдельно в прикладных проектах не предусмотрено.
 * @private
 * @category Visual/Panel
 */
export default class UFSPanelHeaderManager extends Component {
    render() {
        return (React.createElement(UFSPanelHeader, {header: this.props.header, hasIcon: this.props.hasIcon, testID: this.props.testID}, this.props.children));
    }
}
const UFSPanelHeader = requireNativeComponent('UFSPanelHeaderView');
//# sourceMappingURL=UFSPanelHeader.js.map