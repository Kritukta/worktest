import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
import { makeUFSErrorFromUserInfo } from '../../../../JSCore/Common/UFSError';
/**
 * Используется для работы со СБОЛ ПРО.
 *
 * @category Visual
 */
export default class UFSSBOLViewerManager extends Component {
    constructor() {
        super(...arguments);
        this.onError = (event) => {
            this.props.onError(makeUFSErrorFromUserInfo(event.nativeEvent.error));
        };
    }
    render() {
        return (React.createElement(UFSSBOLViewer, {startUrl: this.props.startUrl, finishUrl: this.props.finishUrl, onStart: this.props.onStart, onFinish: this.props.onFinish, onError: this.props.onError ? this.onError : undefined, style: this.props.style, testID: this.props.testID}));
    }
}
const UFSSBOLViewer = requireNativeComponent('UFSSBOLViewerView');
//# sourceMappingURL=UFSSBOLViewer.js.map