/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
export interface Props extends React.Props<UFSCenterPageHeaderManager> {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Text`;
     * - `H1`;
     * - `H2`;
     * - `TabSelector`;
     */
    children?: ReactNode;
}
/**
 * Используется как контейнер, находящийся в центральной части навигационного бара.
 * @category Visual/SplitPanel
 */
export default class UFSCenterPageHeaderManager extends Component<Props, {}> {
    render(): JSX.Element;
}
