/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Заголовок кнопки.
     */
    title?: string;
    /**
     * Вызывается при нажатии на кнопку.
     */
    onClick?: () => void;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Представляет собой особую кнопку, используемую для обозначения навигационного перехода на предыдущую страницу.
 * @category Visual/SplitPanel
 */
export default class UFSNavigationBackButtonManager extends Component<Props, {}> {
    render(): JSX.Element;
}
