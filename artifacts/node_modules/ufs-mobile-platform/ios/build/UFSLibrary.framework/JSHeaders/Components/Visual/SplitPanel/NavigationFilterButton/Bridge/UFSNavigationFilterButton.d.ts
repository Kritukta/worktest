/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Число, отображаемое в бейджике.
     *
     * 0 и отрицательные значения приводят к тому, что бейджик будет скрыт.
     */
    count?: number;
    /**
     * Вызывается при нажатии на кнопку.
     */
    onExecute?: () => void;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Представляет собой особую кнопку-иконку, у которой в правом верхнем углу имеется бейджик, показывающий произвольное число.
 * @category Visual/SplitPanel
 */
export default class UFSNavigationFilterButtonManager extends Component<Props, {}> {
    render(): JSX.Element;
}
