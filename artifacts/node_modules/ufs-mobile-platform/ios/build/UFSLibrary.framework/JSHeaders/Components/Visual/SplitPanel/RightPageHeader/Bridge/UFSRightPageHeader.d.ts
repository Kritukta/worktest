/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
export interface Props extends React.Props<UFSRightPageHeaderManager> {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Button` с типом `TEXT`, `TEXT_RED`;
     * - `Button` с вложенным компонентом `Icon`;
     * - `NavigationFilterButton`;
     * - `HintSwitch`;
     */
    children?: ReactNode;
}
/**
 * Используется как контейнер, находящийся в правой части навигационного бара.
 * @category Visual/SplitPanel
 */
export default class UFSRightPageHeaderManager extends Component<Props, {}> {
    render(): JSX.Element;
}
