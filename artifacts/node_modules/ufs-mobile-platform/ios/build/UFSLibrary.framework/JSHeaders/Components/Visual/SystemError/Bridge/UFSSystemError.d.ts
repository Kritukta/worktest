/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Сообщение, описывающее возникшую ошибку.
     */
    errorMessage?: string;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * Используется для вывода критических ошибок, после которых невозможно восстановление работы приложения (т.е. требуется перезапуск).
 * @category Visual
 */
export default class UFSSystemErrorManager extends Component<Props, {}> {
    render(): JSX.Element;
}
