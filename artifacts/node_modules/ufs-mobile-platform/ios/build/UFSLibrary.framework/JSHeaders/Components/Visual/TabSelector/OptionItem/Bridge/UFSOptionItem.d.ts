/// <reference types="react-native" />
/// <reference types="react" />
import React, { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
export interface Props extends React.Props<UFSOptionItemManager> {
    /**
     * Текстовый ключ, присвоенный элементу.
     */
    value: string;
    /**
     * Подпись элемента, которая видна пользователю.
     */
    title?: string;
    /**
     * Атрибут отключает компонент и не позволяет пользователю взаимодействовать с ним.
     */
    disabled?: boolean;
    /**
     * @private
     * Стили React.
     */
    style?: ViewStyle;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * @private
     */
    children?: ReactNode;
}
/**
 * Компонент используется только в качестве дочернего для `TabSelector`.
 * @category Visual/TabSelector
 */
export default class UFSOptionItemManager extends Component<Props, {}> {
    render(): JSX.Element;
}
