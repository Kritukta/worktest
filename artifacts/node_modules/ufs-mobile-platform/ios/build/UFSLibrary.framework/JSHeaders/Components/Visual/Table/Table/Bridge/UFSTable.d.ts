/// <reference types="react-native" />
/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
export interface Props {
    /**
     * Флаг видимости разделителя между рядами таблицы, не влияет на последнюю строку.
     * Подчеркивание последней строки можно добавить, установив `underlineType` для
     * последней `TableRow`. Значение по умолчанию: `true`.
     */
    underlined?: boolean;
    /**
     * &#x1F534; **Deprecated**. Удаляет все встроенные отступы внутри таблицы. Не предназначен для использования
     * с типизированными ячейками.
     */
    noPaddings?: boolean;
    /**
     * С помощью стиля можно жёстко выставить высоту таблицы и задать цвет фона.
     */
    style?: ViewStyle;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `TableHead`, не более одного;
     * - `TableBody`, может быть несколько.
     */
    children?: ReactNode;
}
export interface TableChildProps {
    /**
     * @private
     * Добавляется компонентом `Table`.
     */
    noPaddings?: boolean;
}
/**
 * @category Visual/Table
 * Контейнер для элементов, определяющих содержимое таблицы.
 *
 * `Table` содержит в себе один или больше `TableBody`, а также опциональный `TableHead`. `TableHead` определяет количество колонок (по умолчанию - 1) с помощью компонентов `TableColumn`. `TableBody` содержит один или несколько `TableRow`, которые содержат ячейки `TableCell`, `BaseCell` или `ProfileCell`.
 */
export default class UFSTableManager extends Component<Props, {}> {
    static defaultProps: {
        underlined: boolean;
    };
    render(): JSX.Element;
}
