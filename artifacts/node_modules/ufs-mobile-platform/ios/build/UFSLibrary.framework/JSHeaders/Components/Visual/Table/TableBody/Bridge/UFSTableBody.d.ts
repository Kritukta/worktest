/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { TableChildProps } from '../../Table/Bridge/UFSTable';
export interface Props extends TableChildProps {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `TableRow`
     */
    children?: ReactNode;
}
export interface TableBodyChildProps {
    /**
     * @private
     * Добавляется компонентом `TableBody`.
     */
    noPaddings?: boolean;
}
/**
 * @category Visual/Table
 * Предназначен для хранения одной или нескольких строк таблицы.
 */
export default class UFSTableBodyManager extends Component<Props, {}> {
    render(): JSX.Element;
}
