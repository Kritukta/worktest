/// <reference types="react" />
/// <reference types="react-native" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
export interface Props {
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `Icon`;
     * - `Button`;
     * - `Text`;
     * - `Switch` (свойства `label` и `underlined` игнорируются).
     */
    children?: ReactNode;
}
export interface PrivateProps extends Props {
    style?: ViewStyle;
}
/**
 * Необязательная правая часть ячейки.
 * @category Visual/Table/Base Cells
 */
export default class UFSAccessoryContainerManager extends Component<Props, {}> {
    private childWithDefaultMargins(child);
    private childrenWithFixedLinkMargins(children);
    render(): JSX.Element;
}
