/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `AccessoryContainer`
     * - `MainContainer`
     * - `HeaderContainer`
     */
    children?: ReactNode;
}
/**
 * Типизированная ячейка представляет из себя контейнер, в котором могут размещаться дочерние
 * компоненты `MainContainer`, `AccessoryContainer` и `HeaderContainer`.
 * @category Visual/Table/Base Cells
 */
export default class UFSBaseCellManager extends Component<Props, {}> {
    private mainContainer;
    private accessoryContainer;
    private headerContainer;
    componentWillMount(): void;
    componentWillReceiveProps(nextProps: Props): void;
    handlePropsUpdate: (props: Props) => void;
    render(): JSX.Element;
}
