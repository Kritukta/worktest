/// <reference types="react" />
import { Component, ReactNode } from 'react';
export interface Props {
    /**
    * Заголовок типизированной ячейки таблицы. Высота компонента будет рассчитана на основании этого поля.
    */
    title: string;
    /**
    * Максимальное количество строк, допустимых в заголовке.
    */
    maxTitleLines?: number;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты не допускаются.
     */
    children?: ReactNode;
}
/**
 * Опциональный заголовок типизированной ячейки
 * @category Visual/Table/Base Cells
 */
export default class UFSHeaderContainerManager extends Component<Props, {}> {
    render(): JSX.Element;
}
