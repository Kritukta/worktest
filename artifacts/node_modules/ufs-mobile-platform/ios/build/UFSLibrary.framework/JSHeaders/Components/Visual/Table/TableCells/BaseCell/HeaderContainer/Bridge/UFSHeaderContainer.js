var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Опциональный заголовок типизированной ячейки
 * @category Visual/Table/Base Cells
 */
export default class UFSHeaderContainerManager extends Component {
    render() {
        return (React.createElement(UFSHeaderContainer, __assign({}, this.props, {testID: this.props.testID})));
    }
}
const UFSHeaderContainer = requireNativeComponent('UFSHeaderContainerView');
//# sourceMappingURL=UFSHeaderContainer.js.map