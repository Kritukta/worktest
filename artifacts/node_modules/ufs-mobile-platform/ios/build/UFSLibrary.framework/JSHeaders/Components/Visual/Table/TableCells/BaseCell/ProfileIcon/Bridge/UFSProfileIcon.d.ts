/// <reference types="react-native" />
/// <reference types="react" />
import { Component, ViewStyle, ReactNode } from 'react';
export interface Props {
    /**
     * Имя файла изображения, расположенного в структуре xcode-проекта.
     */
    name?: string;
    /**
     * Идентификатор файла, сохраненного в локальной базе данных.
     */
    fileId?: string;
    /**
     * Адрес изображения в сети.
     */
    url?: string;
    /**
     * Стили React.
     * @private
     */
    style?: ViewStyle;
    /**
     * @private
     */
    children?: ReactNode;
}
export interface PublicProps extends Props {
}
export interface PrivateProps extends Props {
}
/**
 * Компонент предназначен для использования только в `MainContainer`.
 *
 * Имеет свойства для загрузки изображения, аналогичные компоненту `Image`.
 * Если ни одно из свойств не было задано, либо если не удалось загрузить картинку,
 * то вместо аватара отобразятся "инициалы" пользователя
 * (первые буквы двух первых слов основного текста родительского `MainContainer`).
 *
 * @category Visual/Table/Base Cells/Main Container
 */
export default class UFSProfileIconManager extends Component<PublicProps, {}> {
    render(): JSX.Element;
}
