import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
import UFSImage, { ImageResizeMode } from '../../../../../Image/Bridge/UFSImage';
/**
 * Компонент предназначен для использования только в `MainContainer`.
 *
 * Имеет свойства для загрузки изображения, аналогичные компоненту `Image`.
 * Если ни одно из свойств не было задано, либо если не удалось загрузить картинку,
 * то вместо аватара отобразятся "инициалы" пользователя
 * (первые буквы двух первых слов основного текста родительского `MainContainer`).
 *
 * @category Visual/Table/Base Cells/Main Container
 */
export default class UFSProfileIconManager extends Component {
    render() {
        return (React.createElement(UFSProfileIcon, null, 
            React.createElement(UFSImage, {name: this.props.name, fileId: this.props.fileId, url: this.props.url, resizeMode: ImageResizeMode.COVER, isLoading: false})
        ));
    }
}
const UFSProfileIcon = requireNativeComponent('UFSProfileIconView');
//# sourceMappingURL=UFSProfileIcon.js.map