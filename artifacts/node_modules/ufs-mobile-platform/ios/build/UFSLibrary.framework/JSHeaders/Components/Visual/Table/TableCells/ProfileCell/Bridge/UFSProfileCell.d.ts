/// <reference types="react-native" />
/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
export interface Props {
    /**
     * Имя и фамилия, разделенные пробелом.
     */
    title?: string;
    /**
     * Произвольное текстовое поле, например должность.
     */
    subtitle?: string;
    /**
     * Атрибут определяет наличие стрелки в правой части ячейки.
     */
    hasArrow?: boolean;
    /**
     * @private
     * Стили React.
     */
    style?: ViewStyle;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Дочерние компоненты не поддерживаются.
     */
    children?: ReactNode;
}
/**
 * &#x1F534; **Deprecated**. Ячейка таблицы с персональной информацией.
 *
 * #### Особенности использования
 * В ячейке есть свой разделитель, поэтому для правильного отображения свойство таблицы `underlined`
 * должно быть установлено в `false`. В левой части отображаются инициалы, определяемые свойством `title`.
 * @category Visual/Table/Base Cells
 */
export default class UFSProfileCellManager extends Component<Props, {}> {
    static defaultProps: {
        hasArrow: boolean;
    };
    render(): JSX.Element;
}
