var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * &#x1F534; **Deprecated**. Ячейка таблицы с персональной информацией.
 *
 * #### Особенности использования
 * В ячейке есть свой разделитель, поэтому для правильного отображения свойство таблицы `underlined`
 * должно быть установлено в `false`. В левой части отображаются инициалы, определяемые свойством `title`.
 * @category Visual/Table/Base Cells
 */
export default class UFSProfileCellManager extends Component {
    render() {
        return React.createElement(UFSProfileCell, __assign({}, this.props));
    }
    ;
}
UFSProfileCellManager.defaultProps = {
    hasArrow: true
};
const UFSProfileCell = requireNativeComponent('UFSProfileCellView');
//# sourceMappingURL=UFSProfileCell.js.map