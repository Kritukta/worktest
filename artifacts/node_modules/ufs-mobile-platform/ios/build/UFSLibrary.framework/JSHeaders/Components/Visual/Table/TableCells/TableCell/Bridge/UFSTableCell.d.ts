/// <reference types="react-native" />
/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
import { TableRowChildProps } from '../../../TableRow/Bridge/UFSTableRow';
export interface Props extends TableRowChildProps {
    /**
     * Стили React.
     */
    style?: ViewStyle;
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Поддерживается большинство библиотечных компонентов. Не поддерживаются компоненты
     * группы `Grid` и `Table`, в том числе `TableCell` и типизированные ячейки.
     */
    children?: ReactNode;
}
/**
 * @category Visual/Table
 * Ячейка таблицы. Поддерживает размещение компонентов библиотеки.
 *
 * #### Особенности использования
 *
 * В целях повышения производительности настоятельно не рекомендуется использовать
 * `TableCell` для вывода информации, для которой существуют типизированные ячейки.
 */
export default class UFSTableCellManager extends Component<Props, {}> {
    render(): JSX.Element;
}
