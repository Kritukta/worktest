/// <reference types="react-native" />
/// <reference types="react" />
import { Component, ReactNode } from 'react';
import { ViewStyle } from 'react-native';
import { TableChildProps } from '../../Table/Bridge/UFSTable';
export interface Props extends TableChildProps {
    /**
     * Идентификатор для UI-тестирования.
     */
    testID?: string;
    /**
     * Стили React. Свойство добавлено для возможности задания высоты компонента равной нулю. Это требуется
     * в некоторых случаях. Например, когда есть желание реализовать многоколоночную таблицу и задать
     * относительные ширины для каждой колонки, но при этом отображать заголовок не нужно.
     */
    style?: ViewStyle;
    /**
     * Дочерние компоненты могут быть следующих типов:
     * - `TableColumn`.
     */
    children?: ReactNode;
}
export interface TableHeadChildProps {
    /**
     * @private
     * Добавляется компонентом `TableHead`.
     */
    noPaddings?: boolean;
}
/**
 * @category Visual/Table
 * #### Особенности использования
 *
 * В таблице может присутствовать только один заголовочный элемент. Он должен располагаться
 * на первом месте, непосредственно перед первым `TableBody`.
 */
export default class UFSTableHeadManager extends Component<Props, {}> {
    render(): JSX.Element;
}
