import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
/**
 * Компонент-обертка для компонентов Touchable* из состава React Native.
 * Обертка препятствует всплытию событий выше себя, позволяя использовать
 * вложенные Touchable* компоненты.
 *
 * Благодаря реализации на нативном уровне, компонент можно использовать для
 * блокировки срабатывания нативных событий. Например можно препятствовать
 * событию `onPress` компонента `WidgetContainer`.
 * @category Visual
 */
export default class UFSTouchableUnderlayerManager extends Component {
    render() {
        return (React.createElement(UFSTouchableUnderlayer, {style: this.props.style}, this.props.children));
    }
}
const UFSTouchableUnderlayer = requireNativeComponent('UFSTouchableUnderlayerView');
//# sourceMappingURL=UFSTouchableUnderlayer.js.map