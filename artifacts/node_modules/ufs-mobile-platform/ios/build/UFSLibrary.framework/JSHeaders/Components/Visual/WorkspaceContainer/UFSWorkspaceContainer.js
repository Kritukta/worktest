import React, { Component } from 'react';
import { View, StyleSheet } from 'react-native';
/**
 * Легковесный компонент для упрощения вёрстки. Представляет из себя контейнер, содержащий `WorkspacePanel`.
 * WorkspaceContainer создан для использования в качестве корневого визуального компонента приложения. Тем не менее
 * он может применяться в любом месте иерархии отображения и не имеет специальных требований к вложенным компонентам.
 *
 * Компонент не имеет собственного визуального отображения и занимает всё доступное место. Вложенные в него
 * компоненты будут расположены в ряд.
 *
 * Компонент может кастомизироваться стилями, обратите внимание, что пользвательские стили имеют более высокий
 * приоритет, чем стандартные стили компонента.
 *
 * @category Visual/Workspace
 */
export default class UFSWorkspaceContainer extends Component {
    render() {
        return (React.createElement(View, {style: [styles.container, this.props.style]}, this.props.children));
    }
}
const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row'
    }
});
//# sourceMappingURL=UFSWorkspaceContainer.js.map